import { Text, View } from "react-native";

export default function Resources() {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="text-black">Hello, this is the resources page</Text>
    </View>
  );
}
