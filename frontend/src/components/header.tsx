import React from "react";
import { StyleSheet, View, Image, ImageSourcePropType } from "react-native";

export const HeaderTitle = () => {
  return (
    <View>
    </View>
  );
};

// not currently using, keeping just in case we decide to repurpose
// when we transitipon from tabs to something else